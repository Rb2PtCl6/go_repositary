package main

import (
	"fmt"
)
a_dr:=make(type, 9)
b_dr:=make(type, 9)
c_dr:=make(type, 9)
d_dr:=make(type, 9)
e_dr:=make(type, 9)
f_dr:=make(type, 9)
g_dr:=make(type, 9)
func main() {
	var some float64
	fmt.Print("text: ")
	fmt.Scan(&some)
	fmt.Println()
}
